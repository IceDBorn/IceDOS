Enable XML editing via Preferences

Overview XML inside <hyperv></hyperv> add

<vpindex state='on'/>
<runtime state='on'/>
<synic state='on'/>
<stimer state="on">
	<direct state="on"/>
</stimer>
<reset state='on'/>
<vendor_id state='on' value='other'/>
<frequencies state='on'/>
<reenlightenment state='on'/>
<tlbflush state='on'/>
<ipi state='on'/>
<evmcs state='off'/>

Change <memballoon model='virtio'> to <memballoon model='none'>

Add <kvm></kvm> inside <features></features> and then add <hidden state='on'/> inside of it.

Add <ioapic driver='kvm'/> inside <features></features>

Change <clock></clock> to

<clock offset="localtime">
	<timer name="rtc" tickpolicy="catchup"/>
	<timer name="pit" tickpolicy="delay"/>
	<timer name="hpet" present="no"/>
	<timer name="hypervclock" present="yes"/>
	<timer name="tsc" present="yes" mode="native"/>
</clock>