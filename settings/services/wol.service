[Unit]
Description=Wake-on-LAN for %i
Requires=network.target
After=network.target

# Change enp9s0 with your network device
[Service]
ExecStart=/usr/bin/ethtool -s enp9s0 wol g
Type=oneshot

[Install]
WantedBy=multi-user.target