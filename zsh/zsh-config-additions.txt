# Enable provided zsh script
source ~/.promptline.sh
unsetopt PROMPT_SP

# Enable installed plugins
plugins+=
(
  archlinux
  npm
  sudo
  systemd
  zsh-better-npm-completion
  zsh-syntax-highlighting
  zsh-autosuggestions
  gcloud
)

alias update="bash -c 'yay -Syu --noconfirm ; flatpak upgrade'"

# Change "icedborn" to your username if mullvad is installed
alias vpn-on="ssh icedborn@192.168.1.2 -t 'mullvad connect'"
alias vpn-off="ssh icedborn@192.168.1.2 -t 'mullvad disconnect'"
alias vpn="ssh icedborn@192.168.1.2 -t 'mullvad status'"

# Aliases for sudo commands
alias pacman="sudo pacman"
alias systemctl="sudo systemctl"
alias chmod="sudo chmod"

# Enable zsh history up to 100 entries
SAVEHIST=100
HISTFILE=~/.zsh_history